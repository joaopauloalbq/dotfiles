# backend = "glx";
# experimental-backends = true;

# Shadow
shadow = true;
shadow-radius = 15;
shadow-offset-x = -15;
shadow-offset-y = -15;
shadow-opacity = 0.70;

# Fading
fading = true;
fade-in-step = 0.10;
fade-out-step = 0.10;
fade-exclude = [ ]

# Corners
corner-radius = 2;
rounded-corners-exclude = [
    "class_g = 'polybar'",
    "class_g = 'i3bar'"
    #"class_g = 'rofi'",
    #"window_type = 'dock'"
];

#shadow-exclude-reg = "x24+0+0"; # Don't draw shadows in the 24 pixels from the top of the screen.
inactive-dim = 0.2;
